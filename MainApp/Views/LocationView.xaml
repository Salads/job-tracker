<UserControl x:Class="MainApp.Views.LocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:MainApp"
             xmlns:viewModels="clr-namespace:MainApp.ViewModels"
             d:DesignWidth="450">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/TextBoxStyle.xaml"/>
                <ResourceDictionary Source="/Assets/TextButtonStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <UserControl.DataContext>
        <viewModels:LocationViewModel x:Name="ViewModel"/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox Name="locationTextBox" 
                 Grid.Row="0" Grid.Column="0"
                 Style="{StaticResource ErrorTextBoxStyle}"
                 Text="{Binding LocationInput, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Name="verifyLocationButton" 
                Grid.Row="0" Grid.Column="1" 
                Content="Verify"
                Style="{StaticResource TextButtonStyle}"
                IsEnabled="{Binding IsNotValidating}"
                Command="{Binding ValidateLocationCommand}"/>
    </Grid>
</UserControl>
