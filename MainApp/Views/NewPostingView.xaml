<Window x:Class="MainApp.NewPostingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApp"
        xmlns:viewModels="clr-namespace:MainApp.ViewModels"
        mc:Ignorable="d"
        Title="New Job Posting" Height="auto" Width="auto" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <viewModels:NewPostingWindowViewModel x:Name="ViewModel"/>
    </Window.DataContext>
    <StackPanel Orientation="Vertical">
        <Border BorderBrush="Black" BorderThickness="1">
            <Grid Width="auto" ShowGridLines="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <TextBlock FontSize="12" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Job Title</TextBlock>
                <TextBox Grid.Row="0" Grid.Column="1" Width="auto" Height="25" Text="{Binding JobTitle, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock FontSize="12" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Company Name</TextBlock>
                <TextBox Grid.Row="1" Grid.Column="1" Width="auto" Height="25" Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock FontSize="12" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Posting URL</TextBlock>
                <TextBox Grid.Row="2" Grid.Column="1" Width="auto" Height="25" Text="{Binding PostingURL, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock FontSize="12" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Job Type</TextBlock>
                <ComboBox Name="jobTypeCombo" Grid.Row="3" Grid.Column="1" Width="auto" Height="25" SelectedItem="{Binding JobType}"/>

                <TextBlock FontSize="12" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Job Arrangement</TextBlock>
                <ComboBox Name="jobArrangementCombo" Grid.Row="4" Grid.Column="1" Width="auto" Height="25" SelectedItem="{Binding JobArrangement}"/>

                <TextBlock FontSize="12" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Location</TextBlock>
                <TextBox Grid.Row="5" Grid.Column="1" Width="auto" Height="25" Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock FontSize="12" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Description</TextBlock>
                <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="1">
                    <Button Name="descButton" Content="Edit Description" Width="150" Margin="5" Click="descButton_Click"/>
                    <TextBlock Name="descTextBlock" VerticalAlignment="Center" Padding="5" Text="{Binding JobDescriptionLabel}"/>
                </StackPanel>

                <TextBlock FontSize="12" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Padding="5" HorizontalAlignment="Right">Status</TextBlock>
                <ComboBox Name="jobStatusCombo" Grid.Row="7" Grid.Column="1" Width="auto" Height="25" SelectedItem="{Binding JobStatus}"/>
            </Grid>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Name="saveButton" Content="Save" Width="75" Margin="5"
                    Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Name="cancelButton" Content="Cancel" Width="75" Margin="5"
                    Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </StackPanel>
</Window>
